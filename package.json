{
  "name": "zatto-escola",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:ws": "npm --workspace=apps/ws-server run dev",
    "dev:web": "npm --workspace=apps/web run dev",
    "build": "npm --workspace=apps/web run build",
    "setup": "npm --workspace=apps/ws-server run setup"
  }
}
